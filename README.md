

## Styling (Tailwind)
This project uses Tailwind CSS.
- `src/styles.css` includes Tailwind directives
- `tailwind.config.js` points to `./index.html` and `./src/**/*`

If styles look missing:
1) `npm install`
2) Ensure you ran `npm run dev` (Tailwind runs via PostCSS during Vite dev/build)
